# Atlas Memory

Persistent learnings from prior sessions. Append-only; entries are dated.

Format: `- YYYY-MM-DD — <one-line finding>. Citation if relevant.`

---

- 2026-06-18 — Forma 2D viewer toolbar slot for sidebar icons is `override val toolbarButtons = @Composable { ... }` in `android/app/src/main/java/com/plangrid/android/fragments/ElephantMarkupsExtensionFragment.kt:468`. Existing 2D-gated Funnel filter IconButton sits inside `if (is2dView) { Box(Modifier.fillMaxHeight()) { IconButton(...) { AlloyImage.AlloyImageView(...) } } }`. For always-visible icons, place the `Box` OUTSIDE the `is2dView` branch.
- 2026-06-18 — Gradle module path for the Android app is `:android:app` (NOT `:app`). Build with `./gradlew :android:app:assembleAltDebug`, install with `:android:app:installAltDebug`. The repo also has nested modules like `:android:sharedcomponents:app`.
- 2026-06-18 — Drawable `ic_walk_mode.xml` (2.9KB, viewport 18x20, four stroke paths in #3C3C3C) was originally LMV-only. Copied verbatim into `android/app/src/main/res/drawable/` so `ElephantMarkupsExtensionFragment` can reference `R.drawable.ic_walk_mode` without a cross-module dep on `:android:lmv`.
- 2026-06-18 — Prefer `AlloyImage.AlloyImageView(imageResId = R.drawable.foo, contentDescription = "...")` for drawable XML — no `painterResource` import needed. The existing Funnel pattern uses `rememberVectorPainter(image = imageVector)` because `FunnelIcon.inactive` is an `ImageVector`, not a drawable resource. Two different idioms in the same composable block is fine.
- 2026-06-18 — Two devices typically online in dev: physical phone (e.g. `47031FDKD002DS`) and emulator (`emulator-5554`). Use `ANDROID_SERIAL=emulator-5554 ./gradlew ...installAltDebug` to disambiguate; otherwise Gradle errors with "found more than one device".
- 2026-06-18 — CORRECTION to earlier entry: `toolbarButtons` in `ElephantMarkupsExtensionFragment.kt:468` is the TOP-BAR Funnel filter slot, NOT the right-edge markup sidebar. The right-edge vertical markup-tools palette is rendered by `AnnotationToolbar(...)` in `android/annotations/src/main/java/com/plangrid/android/annotations/views/AnnotationToolbar.kt:196` (and its column at `FullToolbar`/`DefaultContainer(isColumn=true)` around `:815`). It is hosted by `ElephantAnnotationsOverlayPresenter.kt:404` (`markupToolbar.setContent { AnnotationToolbar(...) }`) and positioned via the XML ComposeView `@id/markupToolbar` at `android/app/src/main/res/layout/elephant_markups_extension_fragment.xml:31-38` (`layout_alignParentEnd|alignParentBottom`).
- 2026-06-18 — `ElephantExtension` (external lib `com.plangrid.android:elephant:...`) defines 4 slots: `overlayContent`, `toolbarButtons` (top bar), `toolbarThemeProvider`, `toolbarPositioningModifier`. Local `ElephantFragment.kt:781` adds a 5th: `topOverlays: @Composable () -> Unit` invoked at `:394` inside a root `Box(Modifier.fillMaxSize())` above `ElephantViewer`. `topOverlays` is the right slot for icons that must layer above the markup palette without editing the shared `AnnotationToolbar` (used by `MarkupsFileViewerFragment`, `AcsSheetFragment`, `PdfEditorActivity`).
- 2026-06-18 — PinPoint walk-mode POC icon now lives in `topOverlays` at `ElephantMarkupsExtensionFragment.kt:505-521` (always visible, `Modifier.align(Alignment.BottomEnd)`, size 48dp, padded `Dimens.SpacingXS` end / `Dimens.SpacingXXL` bottom). On click: `Timber.d("WalkMode icon tapped")`. APK installed to both `emulator-5554` and `47031FDKD002DS`; devgrid launched without FATAL/crash logcat entries.
- 2026-06-18 — CORRECTION (wrong-viewer mistake): the user's actual sheet viewer is `com.plangrid.android.sheets.AcsSheetActivity` (LEGACY PG Tasks path), confirmed via `adb dumpsys activity activities` → `topResumedActivity=...AcsSheetActivity`. The Elephant `ElephantMarkupsExtensionFragment.kt` path is the MODERN ACC path and is NOT what renders on the user's device for the sheet they were on. Always run `adb dumpsys activity activities | grep topResumedActivity` BEFORE assuming which fragment hosts the screen.
- 2026-06-18 — Slot map for `ElephantMarkupsExtensionFragment.kt`: (a) `toolbarButtons` at `:468` = top-bar Funnel slot; (b) `topOverlays` at `:504` = floating overlay slot above the viewer, NOT part of the markup tools palette. Neither slot lives in the legacy `AcsSheetActivity` path — both are Elephant-only.
- 2026-06-18 — The actual markup-tools vertical sidebar (used by BOTH legacy and modern hosts) is rendered by `AnnotationToolbar` at `android/annotations/src/main/java/com/plangrid/android/annotations/views/AnnotationToolbar.kt:196`. Host wirings: `AcsSheetFragment.kt:1265` (`setupAnnotationToolbar()` → `setContent { AnnotationToolbar(viewModel = toolbarViewModel) }`) for legacy `AcsSheetActivity`; `ElephantAnnotationsOverlayPresenter.kt:404` for modern Elephant; `MarkupsFileViewerFragment` + `PdfEditorActivity` also share this composable. Editing `AnnotationToolbar.kt` injects into all hosts — appropriate for a POC.
- 2026-06-18 — PinPoint walk-mode POC v2: `WalkModeButton` composable added to `AnnotationToolbar.kt` (~line 1455), wired into `FullToolbar` at line 815-822 BEFORE `AnimatedVisibility` so it stays visible even when the toolbar is collapsed. Styled to match `CollapseButton` (line 1416): `toolbar_tool_size` box, `toolbar_icon_size` image, `enabledColor` tint, followed by `HorizontalLineDivider()`. Drawable `ic_walk_mode.xml` copied into `android/annotations/src/main/res/drawable/` (the `:annotations` module cannot reference `:app` resources). String `R.string.walk_mode_content_desc` added to `android/annotations/src/main/res/values/strings.xml`. Build green; both devices (`emulator-5554`, `47031FDKD002DS`) installed cleanly; no FATAL exceptions on devgrid launch.
- 2026-06-18 — Step 2 SAW overlay: SYSTEM_ALERT_WINDOW permission was already declared at `android/app/src/main/AndroidManifest.xml:40` (no manifest change needed). Permission gate lives in `WalkModeOverlayController.onWalkModeTapped()` at `android/app/src/main/java/com/plangrid/android/sheets/walkmode/WalkModeOverlayController.kt:66-72` — checks `Settings.canDrawOverlays(context)` then either calls `showOverlay()` or routes to `Intent(Settings.ACTION_MANAGE_OVERLAY_PERMISSION, Uri.parse("package:$packageName"))` via an `ActivityResultLauncher` registered as a Fragment property at `AcsSheetFragment.kt:548-553` (must be a property — `registerForActivityResult` requires registration before onAttach). Sheet snapshot uses `PixelCopy.request(activity.window, srcRect, fullFrameBitmap, listener, handler)` at `WalkModeOverlayController.kt:158-188` — chose PixelCopy over `View.draw(Canvas)` because `SheetView2` is SurfaceView-backed and `View.draw` returns a black rect for SurfaceView content. WindowManager.LayoutParams config: `TYPE_APPLICATION_OVERLAY` (Android O+) or `TYPE_SYSTEM_ALERT` (legacy), `FLAG_NOT_FOCUSABLE | FLAG_LAYOUT_NO_LIMITS`, `PixelFormat.TRANSLUCENT`, `gravity = Gravity.TOP or Gravity.START`, size = `0.35 * widthPx` x `0.30 * heightPx` (~35%×30% of screen). Toolbar wiring threaded a new `onWalkModeClick: () -> Unit = {}` parameter through `AnnotationToolbar` (both overloads) → `ToolbarAndPopups` → `FullToolbar` → `WalkModeButton.onClick` — default no-op keeps non-AcsSheetFragment hosts (elephant viewer, MarkupsFileViewerFragment) untouched. Drawing is a custom `WalkModeDrawingView extends View` with `onDraw`/`onTouchEvent` accumulating `Path` strokes in overlay-window-local coordinates (no projection back into sheet content space — POC client-only). Verified end-to-end on `emulator-5554`: first tap → settings activity → grant → return → overlay appears top-left with sheet thumbnail; finger drag draws red polyline; X close button removes the WindowManager view cleanly; second tap (permission already granted) skips the settings detour and shows overlay directly. No logcat FATAL exceptions. Screenshot saved at `/tmp/walk-step2-overlay.png`.
- 2026-06-18 — Step 2 v2 (UX fixes): SheetView2 reference is held by `AcsSheetFragment.kt:447 (private var sheetView: SheetView2? = null)` and assigned at `AcsSheetFragment.kt:853 (sheetView = requireView().findViewById(R.id.tile_view))`. Step 2 v2 wires it into the SAW controller at `AcsSheetFragment.kt:859 (walkModeOverlayController?.setSheetContentView(sheetView!!))` right after the existing `loupeView!!.setSheetView(sheetView!!)`. Snapshot pattern: replaced `PixelCopy.request(window, ...)` (full decorView → chrome bleed) with `view.draw(Canvas(bitmap))` against SheetView2 alone — the v1 comment warning about SurfaceView was overcautious; `SheetView2 → PGTileView → com.qozix.tileview.TileView` extends `ViewGroup` and renders bitmap tiles to Canvas, so `View.draw` returns real pixels (not a black rect). Captured bitmap dimensions = SheetView2.width × SheetView2.height in **sheet-view px** (NOT pre-scaled to overlay size). Zoom/pan matrix pattern for a custom View: keep raw bitmap in source px, track `currentZoomOverlay: Float` and `currentPanOffsetOverlayPx: PointF`, in `onDraw` do `canvas.save(); canvas.translate(pan.x, pan.y); canvas.scale(zoom, zoom); canvas.drawBitmap(snapshot, 0f, 0f, paint); canvas.restoreToCount(...)` — translate-then-scale order is important (pan is in overlay px, scale maps source-px → overlay-px from the post-pan origin). Initial centering computed as `pan = (viewportSize - bitmapSize * zoom) / 2` and gated by `hasCenteredInitial` so user-pan deltas survive subsequent `onSizeChanged` calls. Touch handling for pan: ACTION_DOWN stores `lastTouchXY`, ACTION_MOVE adds delta `(event.x - last, event.y - last)` to `currentPanOffsetOverlayPx` and updates `lastTouchXY`, ACTION_UP is a no-op (no stroke to close). No clamping in v2 (POC accepts overscroll). LESSON: when user says "PiP window" / "drag the map", they imply mini-viewer semantics (zoom+pan navigation), NOT literally "draw on the map" — the v1 polyline was a wrong literal read of "draw" verb. Renamed `WalkModeDrawingView` → `WalkModePanView`; old file deleted, new file at `android/app/src/main/java/com/plangrid/android/sheets/walkmode/WalkModePanView.kt`. Build green via `./gradlew :android:app:assembleAltDebug` on the worktree; installed on both `emulator-5554` and `47031FDKD002DS` with no startup crash. APK at `android/app/build/outputs/apk/alt/debug/app-alt-debug.apk`. End-to-end overlay verification (open sheet → tap walk-mode icon) requires a human driver — not blindly scriptable.
- 2026-06-18 — Step 2 v3 (polish): AnnotationToolbar host call sites — `AcsSheetFragment.kt:1289`, `MarkupsFileViewerFragment.kt:944` (PDF/PNG host; uses `binding.fragmentContainerView` as the snapshot source so it captures BOTH the PdfViewerFragment/PhotoViewerFragment content AND the V2AnnotationView markup overlay drawn on top), `ElephantAnnotationsOverlayPresenter.kt:405` (legacy Elephant — NOT wired in v3; its snapshot source would be `content.tileView` but the presenter doesn't own a Fragment so a permission launcher is not trivially attachable — defer if needed), plus the preview `AnnotationToolbar.kt:1974`. Walk-mode wiring pattern (~7 lines per host): (1) `private var walkModeOverlayController: WalkModeOverlayController? = null`; (2) `private val walkModeOverlayPermissionLauncher: ActivityResultLauncher<Intent> = registerForActivityResult(ActivityResultContracts.StartActivityForResult()) { walkModeOverlayController?.onOverlayPermissionResult() }`; (3) in `onCreateView` (before return): `walkModeOverlayController = WalkModeOverlayController(requireActivity()).also { it.attachPermissionLauncher(walkModeOverlayPermissionLauncher) }`; (4) in `onViewCreated` view.post block: `walkModeOverlayController?.setSheetContentView(binding.fragmentContainerView)` (or whatever the host's content view is); (5) in `onDestroyView`: `walkModeOverlayController?.dismiss(); walkModeOverlayController = null`; (6) in the toolbar Compose call: `onWalkModeClick = { walkModeOverlayController?.onWalkModeTapped() }`. Imports: `androidx.activity.result.ActivityResultLauncher`, `androidx.activity.result.contract.ActivityResultContracts`, `com.plangrid.android.sheets.walkmode.WalkModeOverlayController`. Duplication is small (~7 lines, mechanical) — extracted helper not worth it for the POC.
- 2026-06-18 — Step 2 v3 (UX polish): (a) Bottom-left gravity: `WalkModeOverlayController.kt:128-141` — switched `gravity = Gravity.BOTTOM or Gravity.START`, `x = 8dp`, `y = 16dp` (converted via `metricsScreenPx.density * dp`) so the overlay sits comfortably above the system nav bar. (b) Rounded card frame approach: chose manual `onDraw` `clipPath` + stroked `drawRoundRect` over MaterialCardView because the overlay is a WindowManager-added View (not part of a normal view tree) and MaterialCardView's elevation/shadow would clip outside the SAW window — keep it simple. Constants: `cornerRadiusOverlayPx = 4dp`, `strokeWidthOverlayPx = 2dp`, both computed once from `Resources.getSystem().displayMetrics.density` (no theme dep). Stroke color = `Color.argb(180, 0, 0, 0)` (mid-gray, theme-agnostic). Critical detail: inset the clip rect by `halfStroke = strokeWidthOverlayPx / 2f` because Android strokes are centered on the path — without the inset, half the stroke gets clipped off and the corners read jagged. Sequence in `onDraw`: build clipPath → `canvas.save()` → `canvas.clipPath(clipPath)` → drawRect(white backing) → translate+scale+drawBitmap → `canvas.restoreToCount()` → `canvas.drawRoundRect(rectF, r, r, strokePaint)`. Also had to set `FrameLayout` root background to `Color.TRANSPARENT` (was `0xCC222222`) so the dark rectangular backdrop didn't bleed past the rounded corners. Verified on `emulator-5554`: overlay docks bottom-left ~16dp above nav, sheet snapshot visible with markup pins (PL, CL, CM, COR), rounded corners + dark stroke clearly visible, drag-to-pan works (content shifted on swipe), no logcat FATAL/crashes. Screenshot: `/tmp/walk-step2-v3.png`. PDF/PNG host could not be tested end-to-end (test project had `No files uploaded`); user instructed compile + wiring inspection is enough for that fallback.
- 2026-06-18 — PinPoint walk-mode POC v4 (Approach B1, bound-entity filter): added `V2AnnotationView.drawFiltered(canvas, excludeBoundEntityTypes: Set<BoundEntityType>)` at `android/annotations/src/main/java/com/plangrid/android/annotations/views/V2AnnotationView.kt:1724-1745`, mirroring `onDraw` exactly (same `surfaceDimens` guard, same `sceneGroup?.renderNodes?.reversed()` traversal, same `drawNode(node, surfaceContext, canvas)`), but filtering out render-node keys built from `MARKUP_WRAPPER_PREFIX + annotation.uid` for annotations whose `boundEntityType` is in the exclude set. `WalkModeOverlayController.captureSheetContentSnapshot()` (`android/app/src/main/java/com/plangrid/android/sheets/walkmode/WalkModeOverlayController.kt:209-262`) now finds the V2AnnotationView descendant via a recursive class-based scan (NOT `findViewById(R.id.v2_annotation_view)` — the PDF host attaches its V2AnnotationView programmatically without an XML id, so id-based lookup fails on that path), sets its visibility INVISIBLE inside a try/finally, calls `sheetView.draw(bitmapCanvas)` to capture tiles + sibling overlays, restores visibility, then calls `annotationView.drawFiltered(canvas, setOf(BoundEntityType.ISSUE, BoundEntityType.PHOTO))`. Exclude set is a `private companion object` constant; RFI/ASSET/LOCATION/QUEST/CALIBRATION plus all basic markups remain visible. The hide/restore is single-frame and produces a brief flicker on the live sheet — acceptable POC trade-off. Build green (`./gradlew :android:app:assembleAltDebug` BUILD SUCCESSFUL in 54s); APK installed cleanly on both `emulator-5554` and `47031FDKD002DS`.
- 2026-06-18 — PinPoint walk-mode POC v5 R4 (hybrid live composite for auto-refresh): tile-only snapshot + live drawFiltered composite + cross-window OnDrawListener invalidate. Pattern (cite paths): (a) `WalkModeOverlayController.kt:69-75` introduces `liveAnnotationViewRef: WeakReference<V2AnnotationView>` and `liveAnnotationOnDrawListener` that calls `panViewOverlayLocal?.postInvalidateOnAnimation()` — `postInvalidateOnAnimation` was chosen over `postInvalidate()` because it batches to the next vsync frame and is safer when the listener fires inside the V2AnnotationView draw cycle (no recursion into the current pass). (b) `WalkModeOverlayController.kt:172-194` in `showOverlay()`: resolve V2AnnotationView ONCE via `findV2AnnotationViewDescendant(sheetView)`, pass it to the new `captureTileLayerSnapshot(sheetView, annotationView)`, store a WeakReference, wire `panViewOverlayLocal.setAnnotationRenderer { canvas -> liveAnnotationViewRef.get()?.drawFiltered(canvas, EXCLUDED) }`, and `annotationView.viewTreeObserver.addOnDrawListener(liveAnnotationOnDrawListener)`. (c) `WalkModeOverlayController.kt:250-291` `captureTileLayerSnapshot()` now ONLY captures the tile layer with V2AnnotationView temporarily INVISIBLE — it no longer calls `drawFiltered` (annotations are now live). (d) `WalkModeOverlayController.kt:331-359` `dismiss()` detaches the OnDrawListener via `viewTreeObserver.removeOnDrawListener`, clears the WeakReference, and `releaseResources()` nulls the renderer on the pan view so the SAW window can't retain V2AnnotationView. (e) `WalkModePanView.kt:38-56` adds `setAnnotationRenderer(renderer: ((Canvas) -> Unit)?)` storing in `annotationRendererSheetViewPx`. (f) `WalkModePanView.kt:onDraw` invokes the renderer INSIDE the same `translate(pan) + scale(zoom)` matrix immediately after `canvas.drawBitmap(snapshot, 0f, 0f, bitmapPaint)` so annotation coords (sheet-view px) land in the same coordinate space as the tile bitmap. Cross-window safety: V2AnnotationView lives in the host Activity window; WalkModePanView lives in a separate SAW window (`TYPE_APPLICATION_OVERLAY`). Cross-window invalidate is safe (no "draw during draw" warnings), but `postInvalidateOnAnimation()` is still the right choice because it queues to vsync rather than re-entering. Known POC limitation (NOT FIXED — documented for future polish): if the user pans/zooms the main sheet WHILE the overlay is open, the tile bitmap stays frozen but `drawFiltered` reads V2AnnotationView's `surfaceContext` LIVE — so annotations drift relative to the tile bitmap. Defer fix to a future round: subscribe to qozix `ScaleGestureDetector`/zoom listener and re-capture the tile bitmap on settle. Threading: `drawFiltered` reads `sceneGroup.renderNodes` and `annotations` which are mutated on the main thread; `WalkModePanView.onDraw` runs on the main thread (always for `View.onDraw`); the OnDrawListener fires inside the main-thread draw pass. No cross-thread hazard.
- 2026-06-18 — PinPoint walk-mode POC Step 2.5 (select-start-position flow): added a 3-state FSM `WalkModeEntryState { IDLE, AWAITING_POSITION, OVERLAY_SHOWN }` owned by EACH host fragment (NOT the controller — the fragment knows its tap-capture strategy and lifecycle). Controller refactor (`WalkModeOverlayController.kt`): (a) removed `onWalkModeTapped()`; added `checkPermissionOrRequest(): Boolean` (returns true if SAW already granted, else launches settings + returns false) and `showOverlayCenteredOn(centerSheetViewPx: PointF)` (wraps `showOverlay()` + `panViewOverlayLocal?.setInitialCenter(centerSheetViewPx)` AFTER the snapshot is captured); (b) added public `var onPermissionGranted: (() -> Unit)? = null` — `onOverlayPermissionResult()` invokes it INSTEAD OF jumping straight to `showOverlay`, so the fragment can re-enter `AWAITING_POSITION` and let the user pick a position. Centering math in `WalkModePanView.setInitialCenter(centerSheetViewPx: PointF)` and `maybeCenterInitial()`: `panX = viewportW/2 - tx * zoom; panY = viewportH/2 - ty * zoom`, no clamping (over-pan allowed so a near-edge tap still lands dead-center in the mini map). Per-host tap-capture: AcsSheetFragment uses C1 — programmatic transparent `View` added as a SIBLING of `tile_view` inside the ConstraintLayout at `indexOfChild(tile_view) + 1` with `ConstraintLayout.LayoutParams` anchored to tile_view's four edges; `GestureDetector.SimpleOnGestureListener.onSingleTapUp(e)` reads `PointF(e.x, e.y)` which IS sheet-view px (capture view overlays tile_view 1:1, same space the snapshot bitmap uses). MarkupsFileViewerFragment uses C2 — temporarily swap `binding.fragmentTouchView.onInterceptTouchFn` to a function that consumes ALL events (returns `true` from the lambda → `MarkupsTouchIntercepterView.kt:23` skips `dispatchTouchEvent` to the FragmentContainerView, freezing PDF gestures); restore the saved interceptor on exit. PDF coord math mirrors existing `onInterceptorTouch`: `sheetView = touchEvent - (markupsView.getLocationInWindow - touchView.getLocationInWindow)`. Banner: shared helper `WalkModePositionSelector.kt` (single-banner guard pattern from `V2AnnotationView.markupCancellableBanner:191`) — `AlloyToast.Config(isDismissible=true, subtitle=R.string.walk_mode_select_start_position, iconRes=R.drawable.ic_walk_mode)`, BaseCallback maps `DISMISS_EVENT_ACTION` to `onUserCancel`, layoutParams gravity = `BOTTOM or CENTER_HORIZONTAL`. String added at `android/annotations/src/main/res/values/strings.xml:7` ("Select your start position."). Icon "armed" tint: added `isArmed: Boolean = false` param to `WalkModeButton` (`AnnotationToolbar.kt:1475`) — when true wraps the icon Box in `selectableButtonBackground(true)` (same blue used for selected tools, defined at `AnnotationToolbar.kt:1902-1910`); threaded an `isWalkModeArmed: Boolean = false` parameter through `AnnotationToolbar` (both overloads), `ToolbarAndPopups`, and `FullToolbar` — default false keeps non-FSM hosts untouched. Compose-observable mirror in each fragment is `private val walkModeIsArmedComposeState = mutableStateOf(false)` read inside `setupAnnotationToolbar` / `setupToolbarAndToasts` so the toolbar recomposes on every FSM transition. Back-button handling: `requireActivity().onBackPressedDispatcher.addCallback(viewLifecycleOwner, OnBackPressedCallback(true) { cancelWalkModeAwaitingPosition() })` registered on enter, `.remove()` on exit. Teardown order on `onDestroyView`: `tearDownWalkModeAwaitingPositionUi()` BEFORE `walkModeOverlayController?.dismiss()` to release the back callback even if the user navigated away mid-arming. Helper class `WalkModePositionSelector` route (3a in the plan) was chosen over per-host duplication because both fragments needed identical AlloyToast styling AND the singleton-guard pattern; the helper is ~80 LOC and avoids 2 copies of identical banner setup. Build green (`./gradlew :android:app:assembleAltDebug` 1m 46s); APK installed on both `emulator-5554` and `47031FDKD002DS`. Files: WalkModePanView.kt, WalkModeOverlayController.kt, WalkModePositionSelector.kt (NEW), AnnotationToolbar.kt, AcsSheetFragment.kt, MarkupsFileViewerFragment.kt, strings.xml.
- 2026-06-19 — PinPoint walk-mode POC Step 2.6 (user-location dot + overlay-layer abstraction): introduced a tiny rendering-layer abstraction so the mini map composes a fixed front-to-back stack (white backing + tile bitmap + live `drawFiltered` → zero-or-more layers → rounded-rect stroke frame) and the first concrete layer paints the "you are here" marker. Interface (`WalkModeOverlayLayer.kt`, NEW, `internal`): single `draw(canvas, viewportOverlayPx: RectF, bitmapMatrix: Matrix, snapshotSizeSheetViewPx: PointF)` — every layer receives the (translate+scale) matrix that mapped sheet-view px → overlay px AS DATA (not as canvas state), so it can call `Matrix.mapPoints` on its own sheet-view-px anchors and then draw with FIXED pixel sizes that don't grow with zoom. Canvas is at identity transform when layers are invoked but still INSIDE the rounded-rect clip. Implementation (`WalkModeUserLocationLayer.kt`, NEW): nested `data class UserLocation(centerSheetViewPx: PointF, headingDegrees: Float? = null)` — `headingDegrees` is intentionally nullable as a forward-compat hook for the Step 2.7 direction indicator; same layer will own both because they're visually one composed marker. Three stacked solid-fill discs (no strokes — the "rings" emerge from radii differences): outermost black `Color.argb(0x4D, 0, 0, 0)` at `r + 2dp + 1dp`, middle white at `r + 2dp`, inner blue Material 600 `Color.rgb(0x1E, 0x88, 0xE5)` at `r = 8dp` (AlloyUI primary blue not reachable from `:android:app` colors.xml — falls back to Material). Per-frame allocation avoided via reused `mappedAnchorOverlayPx: FloatArray(2)`. Layer is publicly mutable via `setUserLocation(...)` so Step 2.7 can push heading updates without rewiring. `WalkModePanView.kt` changes: (a) declared `private val overlayLayers = mutableListOf<WalkModeOverlayLayer>()`, `private val bitmapMatrixOverlayPx = Matrix()`, `private val snapshotSizeSheetViewPx = PointF()`, `private val viewportRectFOverlayPx = RectF()`; (b) added `internal fun addLayer/removeLayer/clearLayers(layer)` — visibility is `internal` because `WalkModeOverlayLayer` is internal (Kotlin rejects `public fun` exposing `internal` type — first compile attempt failed with "'public' function exposes its 'internal' parameter type"); the View class stays `public` for inflation, the layer-management API is intentionally narrower; (c) refactored `onDraw` to build the matrix once per frame via `bitmapMatrixOverlayPx.setTranslate(pan).preScale(zoom)`, use `canvas.concat(bitmapMatrixOverlayPx)` for the bitmap+annotation pass (replaces the prior imperative `canvas.translate(pan); canvas.scale(zoom)` — produces an IDENTICAL transform because `Matrix.setTranslate(T).preScale(S) = T·S` which is exactly what `canvas.translate; canvas.scale` left on the canvas matrix stack), then AFTER restoring the bitmap save count, iterate `overlayLayers` BEFORE drawing the stroke. Layer pass sits inside the rounded-rect clip so layers can't bleed past the card corners; stroke draws last so it remains the visible frame. `releaseResources()` now also `overlayLayers.clear()`. `WalkModeOverlayController.kt` changes: added `private var userLocationLayer: WalkModeUserLocationLayer? = null`; `showOverlayCenteredOn(centerSheetViewPx)` instantiates the layer, calls `setUserLocation(UserLocation(centerSheetViewPx, headingDegrees = null))`, and `panViewOverlayLocal?.addLayer(layer)` AFTER `setInitialCenter`; `dismiss()` calls `panViewOverlayLocal?.clearLayers()` and nulls the controller's field BEFORE `releaseResources()` (releaseResources also clears defensively — keeps the dependency explicit). KEY COORDINATE-SPACE INSIGHT (verified by mental dry-run before installing): the dot anchor is in SheetView2-viewport px — the SAME space as the captured bitmap and as `centerSheetViewPx` passed to `setInitialCenter` — so it does NOT need the `annotationOffsetSheetViewPx` translate that `drawFiltered` needs. (`drawFiltered` renders in V2AnnotationView-LOCAL px which has the qozix child offset baked in; the dot is OUR own draw at the same anchor we centered on.) Sanity check: with `pan = viewportCenter - anchor * zoom`, mapping `anchor` through `Matrix(translate=pan, scale=zoom)` gives `pan + zoom*anchor = viewportCenter` — dot lands dead-center after `setInitialCenter`. Build green (`./gradlew :android:app:assembleAltDebug` BUILD SUCCESSFUL in 44s) after the visibility fix; APK installed on both `emulator-5554` and `47031FDKD002DS`. End-to-end visual verification deferred: emulator session was on `LandingActivity` (logged out) and the physical device was screen-locked — neither was open to a sheet/PDF. No login was attempted from a background turn. Static verification: file-by-file re-read, matrix algebra checked, draw order traced. No FATAL/runtime errors in emulator logcat after install. Files: WalkModeOverlayLayer.kt (NEW), WalkModeUserLocationLayer.kt (NEW), WalkModePanView.kt, WalkModeOverlayController.kt.
- 2026-06-19 — PinPoint walk-mode coordinate-space contract (canonical — DO NOT REGRESS). The mini-map had a second "wrong tile content under the dot" bug after Step 2.6. Root cause: the prior code pretended (tap viewport-px at tap time) ≡ (bitmap viewport-px at capture time) — that equation only holds if qozix's scrollX/scrollY/mScale doesn't change between tap and `sheetView.draw(canvas)`. Any layout pass (banner dismiss, capture-view removal, fling-settle) in that window broke the assumption. The fix is structural, not another offset variable: established **sheet-px (file space) as the canonical anchor space** for the entire overlay. Sheet-px is invariant to qozix pan/zoom — same units as annotations and as `SheetCoordinateMapper.viewToSheet()`. Transform stack now stated explicitly in `WalkModeOverlayController.kt` class header: `sheet-px ─(M_sheet_to_bitmap)─> bitmap-px ─(M_bitmap_to_overlay = translate(pan) ∘ scale(zoom))─> overlay-px`. The ONLY boundary conversion (sheet-px → bitmap-px) happens at snapshot time, captured INTO a `Matrix` and never recomputed from live state thereafter. Concrete changes (cite file:line as you read): (a) `WalkModeOverlayController.setSheetContent(view, mapper)` now takes a `SheetCoordinateMapper?` — mandatory non-null on the qozix sheet host, null for PDF (identity matrix). Backward-compat `setSheetContentView` shim kept and deprecated. (b) Tap conversion: `AcsSheetFragment` `enterWalkModeAwaitingPosition`'s `onSingleTapUp` now calls `sheetCoordinateMapper?.viewToSheet(e.x, e.y)` and passes the resulting SHEET-px PointF through `confirmWalkModePosition` → `showOverlayCenteredOn`. PDF host passes its FragmentContainerView-local px as-is (identity matrix). (c) `captureTileLayerSnapshot()` builds `M_sheet_to_bitmap` IMMEDIATELY after `sheetView.draw(canvas)`. Construction: `Matrix.setPolyToPoly` with three sheet-px sample points (0,0), (1,0), (0,1) mapped through `SheetCoordinateMapper.sheetToView(_, clampToView=false)` — three samples uniquely determine the affine and the mapper IS the canonical source for sheet-px ↔ viewport-px on qozix. Identity for PDF. (d) `WalkModePanView.setSnapshot(bitmap, sheetToBitmapMatrix, qozixScale)` stores all three; `setInitialCenter(centerSheetPx)` maps sheet-px → bitmap-px FIRST via `sheetToBitmapMatrix.mapPoints`, then computes `pan = viewportCenter - bitmapPx * currentZoomOverlay`. (e) `onDraw` rebuilds `bitmapToOverlayMatrix`, composes `sheetToOverlayMatrix = bitmapToOverlay.preConcat(sheetToBitmap)`, and derives `annotationContentToOverlayMatrix` by `preScale(1/qozixScale)` to cancel the qozix scale baked into M_sheet_to_bitmap (V2AnnotationView paints in scaled-content-px = sheet-px × qozixScale, NOT sheet-px). Bitmap is drawn under `bitmapToOverlayMatrix`, annotations under `annotationContentToOverlayMatrix` — both consume the same source matrix, can NEVER drift apart. (f) `WalkModeOverlayLayer.draw` API changed: receives `sheetToOverlayMatrix: Matrix` (was `bitmapMatrix` + bitmap size). `WalkModeUserLocationLayer` stores `centerSheetPx` (renamed from `centerSheetViewPx`) and maps directly through the composed matrix — no two-step conversion in the layer. Critical Matrix semantics gotcha: `Matrix.preConcat(M)` means `this = this * M`, with points applied left: `(this*M) * p = this * (M * p)` — M is applied FIRST to the point. So `bitmapToOverlay.preConcat(sheetToBitmap)` ≡ "sheetToBitmap then bitmapToOverlay" ≡ sheet-px → overlay-px. ✓ Verification on `emulator-5554` + `47031FDKD002DS` after `./gradlew :android:app:installAltDebug` (BUILD SUCCESSFUL in 19s/28s). RULES (locked):  R1: every coordinate variable must carry its space in the name (sheetPx / bitmapPx / scaledContentPx / overlayPx). R2: ONE boundary conversion sheet-px → bitmap-px lives in `buildSheetToBitmapMatrix`. Do not duplicate. R3: ONE bitmap-px → overlay-px lives in `WalkModePanView.bitmapToOverlayMatrix`. Layers receive it as data. R4: NEVER read live qozix scroll/scale at render time — the snapshotted matrix is the truth. R5: if you add a new space, document the conversion at the boundary and add a helper — do not sprinkle inline math. Files touched: WalkModeOverlayController.kt, WalkModePanView.kt, WalkModeOverlayLayer.kt, WalkModeUserLocationLayer.kt, AcsSheetFragment.kt, MarkupsFileViewerFragment.kt.
- 2026-06-20 — PinPoint walk-mode POC Step 3 (Camera V2 hookup): after `confirmWalkModePosition(pointSheetPx)` opens the mini-map, ALSO launch `CameraActivity` with `CameraFeatureMode.QuickCreate.SingleCapture` so the user is taken straight to the camera while the SAW overlay floats on top. Intent shape mirrors the canonical entry from `ProjectHubFragment.quickCreatePermissionLauncher` (ProjectHubFragment.kt:272-277): `CameraActivity.getLaunchIntent(context, MultiCaptureFetcher.getOutputDirectory(FileSystem(requireContext(), preferences)), projectUid.uidString, CameraFeatureMode.QuickCreate.SingleCapture)`. SAW lifecycle confirmed safe across Activity transition: `WalkModeOverlayController` uses `WindowManager.LayoutParams.TYPE_APPLICATION_OVERLAY` (WalkModeOverlayController.kt:568) which is a separate top-level window not tied to any Activity. `dismiss()` is only invoked from each host fragment's `onDestroyView` (AcsSheetFragment.kt:2366, MarkupsFileViewerFragment.kt:531) and from the in-overlay close button (WalkModeOverlayController.kt:395) — none of these fire on the camera-launch Activity transition (the host Fragment goes through onPause/onStop but NOT onDestroyView). Auto-refresh limitation (DOCUMENTED — not fixed in Step 3): the R4 OnDrawListener (WalkModeOverlayController.kt:134-139) only fires when the host's V2AnnotationView runs its draw cycle; stopped Activity windows don't get redraws, so a marker added by the data layer WHILE the camera is in front will not repaint the mini map. On return from camera, V2AnnotationView resumes drawing and the OnDrawListener fires → mini map repaints with the new marker. Future polish: subscribe the controller directly to `annotationParentMobius.renderDataStream` so refresh is data-driven, independent of host View visibility. No permission gating added in this step — Camera V2 itself runs its own runtime CAMERA permission flow; a proper `RequestMultiplePermissions` gate around `launchQuickCreateCamera()` is a future step. Files: AcsSheetFragment.kt (added 3 imports + `launchQuickCreateCamera()` method, wired into `confirmWalkModePosition`), MarkupsFileViewerFragment.kt (added 4 imports + `launchQuickCreateCamera()` method using `navArgs.projectUid.uidString`, wired into `confirmWalkModePosition`). Module deps already present (`:android:app` -> `:android:camera`, `:android:components`, `:android:core` per android/app/build.gradle.kts:187-189). NOT BUILT YET — Rick handles build/install/test.
- 2026-06-20 — Walk-mode Step 2.7 cone implementation landed in `pinpoint-walk-mode-poc` worktree on branch `shaggi/pinpoint-walk-mode-poc`. Three new files in `android/app/src/main/java/com/plangrid/android/sheets/walkmode/`: `HeadingSource.kt` (internal interface, `StateFlow<Float?>`), `CompassHeadingSource.kt` (TYPE_ROTATION_VECTOR + SENSOR_DELAY_GAME, display-rotation remap on every event, pre-allocated FloatArrays, magnetic-north only — no fallback), `WalkModeUserDirectionLayer.kt` (filled wedge with `RadialGradient` apex-blue → transparent, EMA τ=80ms with wrap-aware delta, `Canvas.rotate` around the dot anchor, NOT in matrix). Controller wiring in `WalkModeOverlayController.kt`: layer added BEFORE the dot via `panView.addLayer(...)`, `MainScope`+`SupervisorJob` controller scope, `collectLatest` forwards heading to layer + `postInvalidateOnAnimation`. Dot blue is Material 600 `Color.rgb(0x1E, 0x88, 0xE5)` per `WalkModeUserLocationLayer.kt:82` — cone matches.
- 2026-06-20 — Canvas-angle convention in walk-mode cone path: Android canvas 0° = +X, sweep clockwise. To make the cone point sheet-up (-Y) before any device-heading rotation, the arc spans `-90° ± halfAngle`. The unrotated cone points up; `canvas.rotate(headingDeg)` after `translate(cx, cy)` then rotates it to compass north. Documented in `WalkModeUserDirectionLayer.kt` cone-path header.
- 2026-06-20 — Controller `controllerScope` (MainScope + SupervisorJob) is created once at construction and intentionally NOT cancelled in `dismiss()`, because the same controller instance can re-show the overlay (cancelling the scope would no-op subsequent `launch`). Only the `headingCollectorJob` is cancelled per dismiss. The scope's lifetime is bound to the controller, which is `Activity`-owned and dies with the activity — acceptable for the POC.

- 2026-06-20 — Implemented capture→quick-create→pushpin POC plumbing end-to-end. New files: `WalkModeTempMarker.kt` (data class), `WalkModePushpinPlacer.kt` (object with FALLBACK_SHEET_PX_PER_METER=100f and computeMarkerSheetPx using (sin, -cos) so heading 0°=north=−Y), `WalkModeTempMarkerLayer.kt` (red ring + dot, drawn between cone and dot). Controller extended with `currentSurfaceUid`, `currentAnchorSheetPx`, `latestHeadingDeg`, `pendingTempMarker`, `tempMarkerLayer`; `showOverlayCenteredOn(centerSheetPx, surfaceUid?)`; new `onCaptureRequested()` / `consumePendingTempMarker()` / `clearTempMarker()` / `hasPendingTempMarker`. Camera launchers in `AcsSheetFragment` + `MarkupsFileViewerFragment` now route through registered `ActivityResultLauncher`, navigate to `R.id.navigate_quick_create` on RESULT_OK with new placement args, clear marker on cancel. Quick Create nav graph (`android/issues/src/main/res/navigation/navigation_issues.xml:540-602`) gained: `hasWalkModePlacement` (bool), `placementSheetUid` (nullable string), `placementSheetName` (nullable string), `placementX/Y` (floats). Mobius `State` got `walkModePlacement: WalkModePlacement?`; `Action.View.ScreenOpened` got optional `walkModePlacement`; `Effect.LinkWalkModePushpin` added (logs intent only — TODO(pinpoint-auto-link) for the real V2AnnotationRepository.saveAnnotations call). `RenderData.isPlacementLocked` drives the disabled placement field; `isPlacementEnabled` is false when locked. `QuickCreateScreenHost.onShowPlacementPicker` short-circuits when locked.
- 2026-06-20 — Critical scope deviation: V2AnnotationRepository.linkIssueAnnotation is private and takes an ExistingIssuePayload that requires a PgbsAnnotation with translation, dimens, viewableInfo, draftingContext — none of which the QuickCreate Mobius graph currently injects. Constructing PgbsAnnotation in-place would also leak the qozix sheet host's annotation pipeline into the issues module. POC compromise: the LinkWalkModePushpin Effect's handler is a Timber.i logger only. The user's issue is still created; they can manually drop the pin via the existing IssueAnnotationFragment path. TODO(pinpoint-auto-link) tracks the real wiring.
- 2026-06-20 — Heading-to-marker math (in `WalkModePushpinPlacer.computeMarkerSheetPx`) uses `dx = r*sin(headingRad)`, `dy = -r*cos(headingRad)` so headingDeg=0 (magnetic north) projects in sheet-up (−Y), matching the cone orientation in `WalkModeUserDirectionLayer` (cone path starts at canvas angle -90° == −Y).
- 2026-06-20 — Z-order in the SAW pan view (bottom→top): cone, temp marker, user dot. Added in that order inside `showOverlayCenteredOn`. The pending temp marker stays visible across the camera → Quick Create transition because the SAW window is TYPE_APPLICATION_OVERLAY (survives Activity transitions) and the controller doesn't clear the layer until `clearTempMarker` is called explicitly from the host's onResume or the camera-launcher's non-OK branch.
- 2026-06-20 — Resolved scope deviation: added public `V2AnnotationRepository.linkWalkModePushpin(issueUid, surfaceUid, sheetPxX, sheetPxY): Result<Unit>` in `android/annotations/src/main/java/com/plangrid/android/annotations/V2AnnotationRepository.kt:706-747`. Internally looks up `sheetRepository.getSheetByUid(...).pugSize` for dimens (falls back to (0,0) if missing — mirrors 3D path in `PlacementRepositoryImpl.createDraftingContext` which also passes 0.0/0.0), builds a fresh PgbsAnnotation via `MarkupCreator.createViaTap(PointerEvent(Vector(sheetPxX, sheetPxY)), PgbsType.ISSUE_STAMP, ...)`, then delegates to the existing private `linkIssueAnnotation` with an `ExistingIssuePayload(issueUid, "", null, false)`. Failures wrapped in `Result.failure` and Timber.w-logged.
- 2026-06-20 — DI: both `:android:annotations` and `:android:issues` are `@ActivityScope` / `ActivityGraph` and `:android:annotations` does NOT depend on `:android:issues`, so adding `implementation(project(":android:annotations"))` to `android/issues/build.gradle.kts:34-36` is cycle-safe. `V2AnnotationRepository` is provided in `android/annotations/src/main/java/com/plangrid/android/annotations/di/V2AnnotationModule.kt:174` at @ActivityScope — directly injectable into `QuickCreateProcessor.Factory`.
- 2026-06-20 — Walk-mode auto-attach is now real (no longer Timber.i-only). `QuickCreateProcessor.linkWalkModePushpin` at `android/issues/src/main/java/com/plangrid/android/issues/quick_create/mobius/QuickCreateProcessor.kt:452-475` calls `v2AnnotationRepository.linkWalkModePushpin(issueUid, sheetUid, sheetPxX, sheetPxY)` inside the existing `handleWithoutActions` block (ioScheduler-bound). The Timber.i trace stays as a one-line debug log; the call is fire-and-forget — failures degrade gracefully (issue exists, user can drop pin manually). The TODO(pinpoint-auto-link) marker has been removed.
- 2026-06-20 — PgbsAnnotation factory for 2D issue stamps: `MarkupCreator.createViaTap(pointerTapEvent = PointerEvent(Vector(x, y)), type = PgbsType.ISSUE_STAMP, z = 0.0, isPublished = true, strokeParams = StrokeParams.initialStrokeParamsFor(PgbsType.ISSUE_STAMP), fillColor = AnnotationColors.defaultFillColor.publishedColor, createdBy, createdByOxygenId, createdAt = Timestamp.now().value, boundEntityType = BoundEntityType.ISSUE)`. Reference: `android/annotations/src/test/java/com/plangrid/android/annotations/V2AnnotationRepositoryPublishByDefaultTest.kt:113-124` (with RECT) and `pgf/legacy/src/commonMain/kotlin/com/plangrid/pgfoundation/viewables/PGFElephantPinLoader.kt:491-513` (3D analogue using `MarkupCreator.create`). Imports live at `com.plangrid.av2foundation.{annotations.{MarkupCreator, AnnotationColors, BoundEntityType}, geometry.Vector, pgbs.{PgbsType, StrokeParams}, tools.PointerEvent, Timestamp}`.
- 2026-06-21 — Cone outer radius bumped from `24f * density` to `41f * density` (×1.71) in `WalkModeUserDirectionLayer.kt:44`. Half-angle 30° unchanged. Comment updated: `~5x the dot's inner-disc radius (~8dp * 5 = ~41dp)`. Visual confirmed working on Pixel 47031FDKD002DS.
- 2026-06-21 — Cross-graph navigation fix: `AcsSheetFragment.findNavController()` is rooted in the sheet's nav graph which does NOT contain `R.id.navigate_quick_create` (that destination lives in `android/issues/src/main/res/navigation/navigation_issues.xml`, which is included as a sub-graph of `navigation_project`). Direct call to `findNavController().navigate(R.id.navigate_quick_create, args)` throws `IllegalArgumentException: navigation destination ... cannot be found from the current destination Destination(...id/acsSheetFragment)`. Fix: route via `IssuesActivity` Intent with `LAUNCH_FLOW = IssuesConstants.WALK_MODE_QUICK_CREATE` (new constant in `android/domain/src/main/java/com/plangrid/android/domain/issues/IssuesConstants.kt`). `IssuesActivity.onCreate` reads launchFlow at `android/app/src/main/java/com/plangrid/android/issues/IssuesActivity.kt:120-126` and `navController.navigate(R.id.navigate_quick_create, Bundle(intent.extras).apply { putBoolean(IS_ISSUE_ACTIVITY_CONTAINED, true) })`. Both `AcsSheetFragment.navigateToQuickCreateAfterWalkModeCapture` and `MarkupsFileViewerFragment.navigateToQuickCreateAfterWalkModeCapture` now build the Intent + `startActivity(intent)` instead of `findNavController().navigate(...)`. Verified `BUILD SUCCESSFUL`.
- 2026-06-21 — FAILED APPROACH (avoid repeating): setting `R.id.fragment_quick_create` as the start destination of `navigation_project` to bypass IssuesMasterDetailFragment instantiation throws `IllegalArgumentException: navigation destination 2131363205 is not a direct child of this NavGraph` because `fragment_quick_create` is nested inside the `<include app:graph="@navigation/navigation_issues" />` sub-graph (`navigation_project.xml:23`), not a direct child of `navigation_project`. Must keep `issues_navigation` as start destination and rely on explicit `navController.navigate(navigate_quick_create, ...)` after setGraph.
- 2026-06-21 — `WalkModeBridge` singleton at `android/domain/src/main/java/com/plangrid/android/domain/walkmode/WalkModeBridge.kt` — process-scoped cross-Activity signal with `@Volatile var awaitingCameraReopen: Boolean = false`. Placed in `:android:domain` so both `:android:app` (sheet/PDF hosts) and `:android:issues` (Quick Create) can reference it without circular dependency. `:android:issues` already depends on `:android:domain` (`android/issues/build.gradle.kts:39-40`). Set by QuickCreateFragment when walk-mode-flagged issue creation completes; consumed by `AcsSheetFragment.onResume` + `MarkupsFileViewerFragment.onResume` to re-launch the camera. Cleanest cross-Activity signal that doesn't require switching from `startActivity` to `startActivityForResult`.
- 2026-06-21 — TABLET MASTER-DETAIL AUTO-SELECT TRAP (BUG MAGNET — beware on tablet/foldable). `IssuesMasterDetailFragment.onViewCreated` at `android/issues/src/main/java/com/plangrid/android/issues/masterdetail/IssuesMasterDetailFragment.kt:259-264` installs a permanent collector: `viewModel.pendingDetailIssueUid.filterNotNull().collect { openDetails(it) }`. Any code that calls `MasterDetailViewModel.openDetails(uid)` on the activity-scoped VM (created with `activityViewModels({ requireActivity() })`) triggers an `IssueDetailsV2Fragment` instantiation in the split-view detail pane — including from QuickCreateFragment's tablet auto-select branches at `QuickCreateFragment.kt:218 / :262 / :281`. There's also a setter at `IssuesListFragment.kt:274`. When IssuesActivity is launched directly into Quick Create (LAUNCH_FLOW = WALK_MODE_QUICK_CREATE) the project-level NavController has no back-stack entry for the issues_navigation sub-graph yet, so the auto-rendered IssueDetailsV2Fragment NPEs at `IssueDetailsV2Fragment.kt:185` (`projectLevelNavController.currentBackStackEntry!!`). Mitigation pattern: gate every call site to `MasterDetailViewModel.openDetails` with a walk-mode check that returns early.
- 2026-06-21 — `IssueDetailsV2Fragment.kt:185` NPE crash signature: `java.lang.NullPointerException at com.plangrid.android.issues.details.IssueDetailsV2FragmentKt.getIssueReferencesSectionConfig(IssueDetailsV2Fragment.kt:185)`. Line 185 is `backStackEntry = projectLevelNavController.currentBackStackEntry!!`. The `!!` blows up whenever this fragment is instantiated before the project-level NavController has a populated back-stack — happens when IssuesActivity is launched directly into a deep destination via setGraph + navigate without the normal master-detail bootstrap. Prevention is at the SOURCE (don't instantiate IssueDetailsV2Fragment), not the symptom (don't change the `!!` — that just hides the bug).
- 2026-06-21 — UNRESOLVED: Review tap still triggers `QuickCreateFragment.kt:281` (the inside-the-`if (navArgs.hasWalkModePlacement)` early-return is at line 234 and should prevent reaching 281). Diagnostic stack from `Timber.e(Throwable("[walkmode-diag] MasterDetailViewModel.openDetails called"))` confirms: `MasterDetailViewModel.openDetails$ at MasterDetailViewModel.kt:52 ← QuickCreateFragment.createEventHandler$lambda$5(QuickCreateFragment.kt:281)`. This means `navArgs.hasWalkModePlacement` evaluates FALSE when the lambda runs, despite IssuesActivity passing it through `Bundle(intent.extras).apply { putBoolean(IS_ISSUE_ACTIVITY_CONTAINED, true) }` and the original AcsSheetFragment putting `putExtra("hasWalkModePlacement", true)`. Next-session debug step: add `Timber.i("hasWalkModePlacement=${navArgs.hasWalkModePlacement} placementSheetUid=${navArgs.placementSheetUid}")` AT THE TOP of the `onNavigateToIssueDetails` lambda before the gate check. If false, inspect the args bundle that reached QuickCreateFragment — likely the bundle keys are not being preserved through setGraph's start-destination-args + the subsequent navigate's args bundle.
- 2026-06-21 — Temp marker timing fix: previously `WalkModeOverlayController.onCaptureRequested()` was called inside `launchQuickCreateCamera()` (BEFORE camera Activity started), so the marker rendered at the moment the camera opened — wrong UX (heading captured at launch, not at capture; marker visible before the user has decided to commit). NEW: `onCaptureRequested()` is invoked from the `walkModeCameraLauncher` `RESULT_OK` branch in both `AcsSheetFragment.kt:579-583` and `MarkupsFileViewerFragment.kt:389-393`. Heading + 1m projection now reflect the moment the user taps the camera's capture button. Removed the early `onCaptureRequested()` from `launchQuickCreateCamera()` in both hosts. Pair: `onCaptureRequested()` returns the marker AND stores it as `pendingTempMarker`; the next line `consumePendingTempMarker()` retrieves it for nav args. Both calls are needed because the layer still reads `pendingTempMarker` until `clearTempMarker()` fires.
- 2026-06-21 — Defensive change: `WalkModeOverlayController.onCaptureRequested()` now falls back to `headingDeg = 0f` if `latestHeadingDeg` is null (compass hasn't emitted yet) instead of returning null. Reason: without this, the marker silently fails to render on rapid-tap before the first rotation-vector event arrives. The cone uses the live filtered angle (which stays null until the sensor fires); only the marker uses the fallback because the marker only needs *a* heading for the 1m projection, even if it's stale.
- 2026-06-21 — Diagnostic technique that worked: when a callback's early-return clearly should fire but doesn't, instrument the SUSPECTED no-op site with `Timber.e(Throwable("..."), "...")` to capture the actual stack. Then re-run, grep `adb logcat -d | grep "<tag>" -A 30`. The throwable trace pinpoints the exact call chain including Kotlin's `$lambda$N` mangling. Used here to confirm `MasterDetailViewModel.openDetails` was being called from `QuickCreateFragment.kt:281` despite the walk-mode early return at line 234 — meaning the gate evaluated false.
